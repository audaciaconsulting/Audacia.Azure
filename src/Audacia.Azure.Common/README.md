# Audacia.Common

This project contains common code used by `Audacia.Azure.BlobStorage` allowing extension of the `ReturnOptions` interfaces/ classes for custom implementations without adding the dependencies of `Audacia.Azure.BlobStorage`.